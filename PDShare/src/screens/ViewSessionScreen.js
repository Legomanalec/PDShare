import React, { TouchableOpacity } from "react";
import { Text } from "react-native";


const ViewSessionScreen = () => {
  return (
    <Text>Current session where you watch the host</Text>
  );
};

export default ViewSessionScreen;